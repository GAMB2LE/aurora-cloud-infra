---
- name: Deploy Aurora mobile API without changing data-ingest schedules
  hosts: aurora_cloud
  become: true
  roles:
    - release_snapshot
    - dashboard_app
    - dashboard_services
    - nginx
