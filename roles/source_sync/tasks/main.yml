---
- name: Ensure Aurora state directory exists
  ansible.builtin.file:
    path: "{{ aurora_state_root }}"
    state: directory
    owner: "{{ aurora_service_user }}"
    group: "{{ aurora_service_group }}"
    mode: "0755"
  when: cl61_source_sync_enabled | bool or radar_source_sync_enabled | bool or vaisalamet_source_sync_enabled | bool or asfs_logger_source_sync_enabled | bool or asfs_fast_sonic_source_sync_enabled | bool or power_source_sync_enabled | bool or wxcam_source_sync_enabled | bool

- name: Ensure Aurora SSH directory exists
  ansible.builtin.file:
    path: "/home/{{ aurora_service_user }}/.ssh"
    state: directory
    owner: "{{ aurora_service_user }}"
    group: "{{ aurora_service_group }}"
    mode: "0700"
  when: cl61_source_sync_enabled | bool or radar_source_sync_enabled | bool or vaisalamet_source_sync_enabled | bool or asfs_logger_source_sync_enabled | bool or asfs_fast_sonic_source_sync_enabled | bool or power_source_sync_enabled | bool or wxcam_source_sync_enabled | bool

- name: Install CL61 source SSH private key when provided by vault
  ansible.builtin.copy:
    content: "{{ cl61_source_ssh_private_key_content }}"
    dest: "{{ cl61_source_ssh_key_path }}"
    owner: "{{ aurora_service_user }}"
    group: "{{ aurora_service_group }}"
    mode: "0600"
  no_log: true
  when:
    - cl61_source_sync_enabled | bool
    - cl61_source_ssh_private_key_content is defined
    - cl61_source_ssh_private_key_content | length > 0

- name: Generate CL61 source SSH key when no vault key is provided
  ansible.builtin.command: "ssh-keygen -t ed25519 -N '' -f {{ cl61_source_ssh_key_path }} -C aurora-cl61-source-sync@{{ inventory_hostname }}"
  args:
    creates: "{{ cl61_source_ssh_key_path }}"
  become_user: "{{ aurora_service_user }}"
  when:
    - cl61_source_sync_enabled | bool
    - cl61_source_generate_ssh_key | bool
    - cl61_source_ssh_private_key_content is not defined or cl61_source_ssh_private_key_content | length == 0
    - not ansible_check_mode

- name: Scan CL61 source SSH host key
  ansible.builtin.command: "ssh-keyscan -T 10 {{ cl61_source_host }}"
  changed_when: false
  register: cl61_source_keyscan
  when:
    - cl61_source_sync_enabled | bool
    - not ansible_check_mode

- name: Trust CL61 source SSH host key for Aurora service user
  ansible.builtin.known_hosts:
    path: "/home/{{ aurora_service_user }}/.ssh/known_hosts"
    name: "{{ cl61_source_host }}"
    key: "{{ item }}"
    state: present
  loop: "{{ cl61_source_keyscan.stdout_lines | default([]) }}"
  when:
    - cl61_source_sync_enabled | bool
    - not ansible_check_mode

- name: Ensure CL61 source known_hosts ownership
  ansible.builtin.file:
    path: "/home/{{ aurora_service_user }}/.ssh/known_hosts"
    owner: "{{ aurora_service_user }}"
    group: "{{ aurora_service_group }}"
    mode: "0644"
  when:
    - cl61_source_sync_enabled | bool
    - not ansible_check_mode
    - cl61_source_keyscan.stdout_lines | default([]) | length > 0

- name: Install CL61 source sync script
  ansible.builtin.template:
    src: aurora-cl61-sync.j2
    dest: /usr/local/bin/aurora-cl61-sync
    owner: root
    group: root
    mode: "0755"
  when: cl61_source_sync_enabled | bool

- name: Install CL61 source sync systemd units
  ansible.builtin.template:
    src: "{{ item }}.j2"
    dest: "/etc/systemd/system/{{ item }}"
    owner: root
    group: root
    mode: "0644"
  loop:
    - aurora-cl61-source-sync.service
    - aurora-cl61-source-sync.timer
  when: cl61_source_sync_enabled | bool

- name: Enable CL61 source sync timer
  ansible.builtin.systemd:
    name: aurora-cl61-source-sync.timer
    enabled: true
    state: started
    daemon_reload: true
  when:
    - cl61_source_sync_enabled | bool
    - cl61_source_sync_timer_enabled | bool
    - not ansible_check_mode

- name: Disable CL61 source sync timer when requested
  ansible.builtin.systemd:
    name: aurora-cl61-source-sync.timer
    enabled: false
    state: stopped
    daemon_reload: true
  when:
    - not cl61_source_sync_enabled | bool or not cl61_source_sync_timer_enabled | bool
    - not ansible_check_mode

- name: Install radar source sync script
  ansible.builtin.template:
    src: aurora-radar-sync.j2
    dest: /usr/local/bin/aurora-radar-sync
    owner: root
    group: root
    mode: "0755"
  when: radar_source_sync_enabled | bool

- name: Install radar source sync systemd units
  ansible.builtin.template:
    src: "{{ item }}.j2"
    dest: "/etc/systemd/system/{{ item }}"
    owner: root
    group: root
    mode: "0644"
  loop:
    - aurora-radar-source-sync.service
    - aurora-radar-source-sync.timer
  when: radar_source_sync_enabled | bool

- name: Enable radar source sync timer
  ansible.builtin.systemd:
    name: aurora-radar-source-sync.timer
    enabled: true
    state: started
    daemon_reload: true
  when:
    - radar_source_sync_enabled | bool
    - radar_source_sync_timer_enabled | bool
    - not ansible_check_mode

- name: Disable radar source sync timer when requested
  ansible.builtin.systemd:
    name: aurora-radar-source-sync.timer
    enabled: false
    state: stopped
    daemon_reload: true
  when:
    - not radar_source_sync_enabled | bool or not radar_source_sync_timer_enabled | bool
    - not ansible_check_mode

- name: Install Vaisala met source sync script
  ansible.builtin.template:
    src: aurora-vaisalamet-sync.j2
    dest: /usr/local/bin/aurora-vaisalamet-sync
    owner: root
    group: root
    mode: "0755"
  when: vaisalamet_source_sync_enabled | bool

- name: Install Vaisala met source sync systemd units
  ansible.builtin.template:
    src: "{{ item }}.j2"
    dest: "/etc/systemd/system/{{ item }}"
    owner: root
    group: root
    mode: "0644"
  loop:
    - aurora-vaisalamet-source-sync.service
    - aurora-vaisalamet-source-sync.timer
  when: vaisalamet_source_sync_enabled | bool

- name: Enable Vaisala met source sync timer
  ansible.builtin.systemd:
    name: aurora-vaisalamet-source-sync.timer
    enabled: true
    state: started
    daemon_reload: true
  when:
    - vaisalamet_source_sync_enabled | bool
    - vaisalamet_source_sync_timer_enabled | bool
    - not ansible_check_mode

- name: Disable Vaisala met source sync timer when requested
  ansible.builtin.systemd:
    name: aurora-vaisalamet-source-sync.timer
    enabled: false
    state: stopped
    daemon_reload: true
  when:
    - not vaisalamet_source_sync_enabled | bool or not vaisalamet_source_sync_timer_enabled | bool
    - not ansible_check_mode

- name: Install ASFS LoggerNet source sync script
  ansible.builtin.template:
    src: aurora-asfs-logger-sync.j2
    dest: /usr/local/bin/aurora-asfs-logger-sync
    owner: root
    group: root
    mode: "0755"
  when: asfs_logger_source_sync_enabled | bool

- name: Install ASFS LoggerNet source sync systemd units
  ansible.builtin.template:
    src: "{{ item }}.j2"
    dest: "/etc/systemd/system/{{ item }}"
    owner: root
    group: root
    mode: "0644"
  loop:
    - aurora-asfs-logger-source-sync.service
    - aurora-asfs-logger-source-sync.timer
  when: asfs_logger_source_sync_enabled | bool

- name: Enable ASFS LoggerNet source sync timer
  ansible.builtin.systemd:
    name: aurora-asfs-logger-source-sync.timer
    enabled: true
    state: started
    daemon_reload: true
  when:
    - asfs_logger_source_sync_enabled | bool
    - asfs_logger_source_sync_timer_enabled | bool
    - not ansible_check_mode

- name: Disable ASFS LoggerNet source sync timer when requested
  ansible.builtin.systemd:
    name: aurora-asfs-logger-source-sync.timer
    enabled: false
    state: stopped
    daemon_reload: true
  when:
    - not asfs_logger_source_sync_enabled | bool or not asfs_logger_source_sync_timer_enabled | bool
    - not ansible_check_mode

- name: Install ASFS fast-sonic source sync script
  ansible.builtin.template:
    src: aurora-asfs-fast-sonic-sync.j2
    dest: /usr/local/bin/aurora-asfs-fast-sonic-sync
    owner: root
    group: root
    mode: "0755"
  when: asfs_fast_sonic_source_sync_enabled | bool

- name: Install ASFS fast-sonic source sync systemd units
  ansible.builtin.template:
    src: "{{ item }}.j2"
    dest: "/etc/systemd/system/{{ item }}"
    owner: root
    group: root
    mode: "0644"
  loop:
    - aurora-asfs-fast-sonic-source-sync.service
    - aurora-asfs-fast-sonic-source-sync.timer
  when: asfs_fast_sonic_source_sync_enabled | bool

- name: Enable ASFS fast-sonic source sync timer
  ansible.builtin.systemd:
    name: aurora-asfs-fast-sonic-source-sync.timer
    enabled: true
    state: started
    daemon_reload: true
  when:
    - asfs_fast_sonic_source_sync_enabled | bool
    - asfs_fast_sonic_source_sync_timer_enabled | bool
    - not ansible_check_mode

- name: Disable ASFS fast-sonic source sync timer when requested
  ansible.builtin.systemd:
    name: aurora-asfs-fast-sonic-source-sync.timer
    enabled: false
    state: stopped
    daemon_reload: true
  when:
    - not asfs_fast_sonic_source_sync_enabled | bool or not asfs_fast_sonic_source_sync_timer_enabled | bool
    - not ansible_check_mode

- name: Install power source sync script
  ansible.builtin.template:
    src: aurora-power-sync.j2
    dest: /usr/local/bin/aurora-power-sync
    owner: root
    group: root
    mode: "0755"
  when: power_source_sync_enabled | bool

- name: Install power source sync systemd units
  ansible.builtin.template:
    src: "{{ item }}.j2"
    dest: "/etc/systemd/system/{{ item }}"
    owner: root
    group: root
    mode: "0644"
  loop:
    - aurora-power-source-sync.service
    - aurora-power-source-sync.timer
  when: power_source_sync_enabled | bool

- name: Enable power source sync timer
  ansible.builtin.systemd:
    name: aurora-power-source-sync.timer
    enabled: true
    state: started
    daemon_reload: true
  when:
    - power_source_sync_enabled | bool
    - power_source_sync_timer_enabled | bool
    - not ansible_check_mode

- name: Disable power source sync timer when requested
  ansible.builtin.systemd:
    name: aurora-power-source-sync.timer
    enabled: false
    state: stopped
    daemon_reload: true
  when:
    - not power_source_sync_enabled | bool or not power_source_sync_timer_enabled | bool
    - not ansible_check_mode

- name: Install wxcam source sync script
  ansible.builtin.template:
    src: aurora-wxcam-sync.j2
    dest: /usr/local/bin/aurora-wxcam-sync
    owner: root
    group: root
    mode: "0755"
  when: wxcam_source_sync_enabled | bool

- name: Install wxcam source sync systemd units
  ansible.builtin.template:
    src: "{{ item }}.j2"
    dest: "/etc/systemd/system/{{ item }}"
    owner: root
    group: root
    mode: "0644"
  loop:
    - aurora-wxcam-source-sync.service
    - aurora-wxcam-source-sync.timer
  when: wxcam_source_sync_enabled | bool

- name: Enable wxcam source sync timer
  ansible.builtin.systemd:
    name: aurora-wxcam-source-sync.timer
    enabled: true
    state: started
    daemon_reload: true
  when:
    - wxcam_source_sync_enabled | bool
    - wxcam_source_sync_timer_enabled | bool
    - not ansible_check_mode

- name: Disable wxcam source sync timer when requested
  ansible.builtin.systemd:
    name: aurora-wxcam-source-sync.timer
    enabled: false
    state: stopped
    daemon_reload: true
  when:
    - not wxcam_source_sync_enabled | bool or not wxcam_source_sync_timer_enabled | bool
    - not ansible_check_mode
