[Unit]
Description=Schedule Aurora mirror verification and prune-gate manifests

[Timer]
OnCalendar={{ gws_verification_schedule }}
Persistent=true
RandomizedDelaySec={{ gws_verification_randomized_delay_sec | default(300) }}
Unit=aurora-mirror-verify.service

[Install]
WantedBy=timers.target
